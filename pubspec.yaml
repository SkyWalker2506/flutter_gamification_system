name: flutter_gamification_system
description: "Gamification engine for Flutter — XP, levels, streaks, badges, achievements, SRS rating."
version: 0.1.0
homepage: https://github.com/SkyWalker2506/flutter_gamification_system

environment:
  sdk: ^3.11.3
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
